# Conexão com o Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/investtrack

# Segredo para assinar os tokens JWT (troque em produção!)
JWT_SECRET=troque-este-segredo
JWT_EXPIRES=7d

# Porta da API
PORT=3333

# Origem permitida no CORS (o front do Vite)
CORS_ORIGIN=http://localhost:5173
